# Example env file for frontend (create .env.local or set in Vercel env vars)
# Do NOT commit real secrets to git.

# API base URL for backend (must include /api if backend serves API at that path)
REACT_APP_API_URL=https://dvooskid.pythonanywhere.com/api

# Any additional client-side config
# e.g. REACT_APP_SENTRY_DSN=your-sentry-dsn

# Note: Vercel environment variables should be set in the Vercel dashboard under Project Settings -> Environment Variables.
